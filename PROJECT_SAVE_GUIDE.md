# 💾 プロジェクト保存・読み込みガイド

平面図描画アプリでは、作成中の図面をプロジェクトファイルとして保存し、後で続きから作業することができます。

## 📤 プロジェクトの保存方法

### 1. エクスポートメニューから保存
1. 描画画面の上部にある **📤 エクスポート** ボタンをクリック
2. メニューが表示されたら **💾 プロジェクト** を選択
3. 自動的にJSONファイルがダウンロードされます

### 2. 保存されるファイル
- **ファイル名**: `floorplan-project-YYYYMMDD-HHMMSS.json`
- **形式**: JSON（テキスト形式）
- **内容**:
  - 全ての描画データ（線、図形、テキストなど）
  - キャンバス設定（グリッドサイズ、表示倍率など）
  - プロジェクト統計情報

## 📂 プロジェクトの読み込み方法

### 1. 初期画面から読み込み
1. アプリを開くと表示される初期画面で **「途中から作成する」** をクリック
2. ファイル選択ダイアログが開きます
3. 保存したJSONファイル（`.json`）を選択
4. プロジェクトが復元され、続きから作業できます

### 2. 読み込める形式
- **📄 JSONファイル（.json）**: 保存したプロジェクトデータ
- **🖼️ 画像ファイル（.png, .jpg, .jpeg）**: 背景画像として読み込み

## 🔄 自動保存機能

アプリには自動保存機能があり、作業中のデータは自動的にブラウザに保存されます：

- **保存タイミング**: 描画操作を行うたびに自動保存
- **保存場所**: ブラウザのローカルストレージ
- **復元方法**: 次回アプリを開いた時に「前回の続きから」として表示

## 📋 プロジェクトファイルの中身

保存されたJSONファイルには以下の情報が含まれます：

```json
{
  "version": "1.0",
  "timestamp": "2025-01-XX...",
  "settings": {
    "gridSize": 160,
    "scale": 1.0,
    "offsetX": 0,
    "offsetY": 0,
    "snapToGrid": true
  },
  "paths": [
    {
      "tool": "rect",
      "startPoint": {"x": 100, "y": 100},
      "endPoint": {"x": 400, "y": 300},
      "strokeColor": "#000000",
      "strokeWidth": 4
    }
  ],
  "statistics": {
    "totalPaths": 5,
    "pathsByTool": {
      "rect": 2,
      "line": 1,
      "door": 2
    }
  }
}
```

## 💡 使用シーン

### 📱 途中保存して別のデバイスで続き
1. タブレットで図面作成を開始
2. プロジェクトファイルとして保存
3. PCで同じファイルを読み込んで詳細を追加

### 🏠 複数のプロジェクト管理
1. 各部屋ごとにプロジェクトファイルを作成
2. 用途別にファイルを整理
3. 必要な時に該当ファイルを読み込み

### 📋 テンプレート作成
1. よく使う間取りのベースを作成
2. プロジェクトファイルとして保存
3. 新しいプロジェクトのテンプレートとして活用

## ⚠️ 注意点

### ファイル管理
- JSONファイルは分かりやすいファイル名に変更可能
- クラウドストレージに保存すればデバイス間で共有可能
- バックアップとして複数箇所に保存することを推奨

### 互換性
- プロジェクトファイルは将来のバージョンでも読み込み可能
- 古いバージョンで新しい機能のデータは一部表示されない場合があります

### ブラウザの制限
- 自動保存はブラウザごとに個別管理
- ブラウザのデータクリアで自動保存データは消失
- 重要なプロジェクトは必ずファイル保存することを推奨

---

## 🚀 実際の使い方例

1. **新規プロジェクト開始**
   - 初期画面で「新規作成」→ 図面作成

2. **途中保存**
   - エクスポート → プロジェクト → JSONファイルダウンロード

3. **継続作業**
   - 初期画面で「途中から作成する」→ JSONファイル選択

4. **完成時**
   - エクスポート → PDF/画像で最終出力
   - プロジェクトファイルも保存（将来の修正用）

これで作成中の平面図を安全に保存し、いつでも続きから作業できます！
